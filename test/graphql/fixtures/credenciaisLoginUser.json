{
    "query": "mutation Login($username: String!, $password: String!) {login(username: $username, password: $password) {token user {username}}}",
    "variables": {
        "username": "alle",
        "password": "123456"
    }
}